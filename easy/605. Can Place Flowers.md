# [605. Can Place Flowers](https://leetcode.com/problems/can-place-flowers/description/?envType=study-plan-v2&envId=leetcode-75)

## Intuition
Since flowers cannot be planted in adjacent positions, we check for each index whether `the current position and its neighbors (left and right) are all empty`.<br>
If the condition is satisfied, we plant a flower at that position and decrease the remaining number of flowers `n`.

---

## Approach
1. Iterate through the array from start to end. 
2. For each index `i`:
   - The current position must be 0, and 
     - The left position is empty (`i == 0` or `flowerbed[i - 1] == 0`)
     - The right position is empty (`i == n - 1` or `flowerbed[i + 1] == 0`)
3. If all conditions are met, plant a flower at that position and decrement `n`. 
4. After the iteration, return `true` if `n <= 0`, otherwise return `false`.

---

## Complexity
### Time Complexity
O(n), as we traverse the array once.

### Space Complexity
O(1), since no extra space is used beyond a few variables.

---

## My Solution
```
class Solution {
    public boolean canPlaceFlowers(int[] flowerbed, int n) {
        boolean result = false;

        if (flowerbed[0] == 0) {
            if (flowerbed.length == 1) {
                n--;

                return (n <= 0) ? true : false;
            } else if (flowerbed[1] == 0) {
                flowerbed[0] = 1;
                n--;
            }
        }

        for (int i = 1; i < flowerbed.length - 1; i++) {
            if (flowerbed[i] == 0) {
                if (flowerbed[i - 1] == 0 && flowerbed[i + 1] == 0) {
                    n--;
                    flowerbed[i] = 1;
                }
            }
        }

        if (flowerbed[flowerbed.length - 1] == 0 && flowerbed[flowerbed.length - 2] == 0 && flowerbed.length > 1) {
            flowerbed[flowerbed.length - 1] = 1;
            n--;
        }

        return (n <= 0) ? true : false;
    }
}
```

---

## Review
Although the original implementation works correctly, it introduces unnecessary complexity in structure.

- Separately handling the beginning, middle, and end → **code duplication and complex branching** 
- Directly modifying `flowerbed` → **harder to trace state during debugging** 
- Nested conditionals reduce readability → can be simplified with `n <= 0`

By treating both ends of the array as if they are surrounded by virtual zeros,<br>
we can handle all positions using a single, unified logic.

## Revised Solution
```
class Solution {
    public boolean canPlaceFlowers(int[] flowerbed, int n) {
        int length = flowerbed.length;

        for (int i = 0; i < length; i++) {
            boolean leftEmpty = (i == 0)            || flowerbed[i - 1] == 0;
            boolean rightEmpty = (i == length - 1)  || flowerbed[i + 1] == 0;

            if (flowerbed[i] == 0 && leftEmpty && rightEmpty) {
                flowerbed[i] = 1;
                n--;
            }
        }

        return n <= 0;
    }
}
```

| Category             | Original                          | Improved                      |
| -------------------- | --------------------------------- | ----------------------------- |
| Boundary Handling    | Separate cases (start/middle/end) | Unified in a single loop      |
| Condition Expression | Nested `if` statements            | Clear with boolean variables  |
| Return Expression    | `(n <= 0) ? true : false`         | `n <= 0`                      |
| Readability          | Multiple branches                 | Simplified and more intuitive |


---

## Performance Comparison

<img width="821" height="276" alt="Screenshot 2026-04-25 at 3 38 19 PM" src="https://github.com/user-attachments/assets/c4addec8-bd35-4c4d-ae84-c27a890c8215" />

This improvement is not about changing the algorithm itself, but about refining the implementation:<br>
**Simplifying the logic leads to cleaner code and naturally better performance.**

Even with the same time complexity,<br>
implementation details can influence actual runtime behavior and memory efficiency.

### Key Improvements
- **Improved Memory Efficiency**
   - The absolute memory usage shows only a slight decrease (47.99 → 47.80 MB),
   - but the percentile ranking significantly improved from **20% to 58%**.
   - → This indicates a more efficient execution pattern overall.
- **Simplified Control Flow**
   - Before: Separate handling for the beginning, middle, and end.
   - After: A single loop with unified boundary handling.
   - → Leads to better branch prediction and cleaner execution.
- **Reduced Conditional Overhead**
   - Removed deeply nested `if` statements.
   - Introduced clear boolean flags (`leftEmpty`, `rightEmpty`).
   - → Fewer comparisons and improved readability.
- **Same Runtime, Better Structure**
   - Execution time remains the same (1 ms),
   - but the improved structure results in more consistent and maintainable performance.
